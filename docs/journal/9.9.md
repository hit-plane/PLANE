# 日报 Day03（日期：2026-09-09）  组：廖尉钧，孙浩凡，孙嘉晟，艾奕舟，霍睿深

## 今日完成

1. 完成 view/controller/model 三层包结构搭建，定义 Model 层对外 `GameModel` 核心接口与各实体类空实现骨架，工程可正常编译；
2. 完成 `ConsoleMain` 无界面驱动验证，确认 Model 层零 JavaFX 引入、可脱离界面独立运行；
3. 绘制完成类图、玩家移动时序图、帧推进时序图、数据流图共 4 份设计图，完成图码一致性核对；
4. 更新 QA 问题表，新增 4 条设计阶段疑问，明确临时处置方案与待确认状态；
5. 完成需求 - 设计跟踪矩阵，覆盖 F01-F16 全量需求，核心功能逐项映射设计元素；
6. 输出《概要设计说明书》v1.0 初稿，包含 10 章完整结构与 AI 使用核对说明；
7. 新建 `feature/preliminary-design` 开发分支，完成全部设计产物提交，合并至 master 并打 tag `v0.2-design`。

## AI 使用记录

表格

| 生成目标 | 提示词要点 | 核对发现 | 处理 |
| --- | --- | --- | --- |
| 三层架构类图 | 分层约束 / 单向依赖 / 禁止 JavaFX / 无过度设计 / 输出 PlantUML | AI 初始生成多余抽象工厂与观察者模式 | 删除冗余设计，精简为核心实体类后采纳 |
| 双场景时序图 | 生命线对应角色 / 消息标注方法名 / 分支覆盖全场景 | 初始链路缺少边界碰撞判定分支 | 补充碰撞判定逻辑分支后采纳 |
| GameModel 接口设计 | 读写操作分组 / 方法语义注释 / 对应需求编号 | 初始方法数量冗余，职责重叠 | 合并同类方法，精简为 9 个核心接口后采纳 |
| 需求 - 设计跟踪矩阵 | F01-F16 逐项映射 / 可延迟项标注 | 部分扩展需求预留了多余接口 | 标记 [可延迟]，移除冗余预留接口后采纳 |

## 问题与风险

- Markdown 文档图片引用路径不匹配，编辑器批量报无法解析警告 → 已统一修正为 `../` 相对路径格式，所有警告消除。
- 远程仓库推送出现 `Connection reset` 网络波动 → 多次重试后推送成功，后续可配置 Git 代理优化稳定性。
- 空实现类初始编译缺少包路径声明 → 已补全对应 package 声明，工程编译正常通过。

## 明日计划

- 实现 Model 层核心业务逻辑：敌机生成移动、碰撞检测、计分与生命值、游戏结束判定；
- 补全 Controller 层事件转发与 View 层基础渲染逻辑，跑通可交互的基础游戏版本；
- 启动详细设计说明书编写，补充类方法细节、数据结构与算法说明。
