# 日报 Day06（日期：2026-09-14）  组：廖尉钧，孙浩凡，孙嘉晟，艾奕舟，霍睿深

## 今日完成
1. 素材资源与窗口适配：新增治疗药水、盾牌、子弹+1、背景、爆炸波等贴图资源并接入 AssetLoader 统一加载；实现窗口按屏幕等比缩放，小屏不再显示不全；
2. 特效系统：新增 HitEffect 受击特效与 BombWave 炸弹道具动画（爆炸波特效贴图），并修复受击特效显示问题；
3. 难度系统：新增 Difficulty 三档难度与 GameConfig 配置驱动，扩展 config.properties，主菜单接入难度选择，难度上调并适配竖版战场；
4. 竖版战场改造：完成竖版渲染改造、难度上调与开始按钮贴图，同步扩展 GameModel 接口与 GameModelImpl 业务逻辑；
5. 道具与状态修复：调整炸弹道具数值；增强盾道具并新增破盾后无敌帧；修复暂停时背景仍滚动、首次开始不显示第一关等 bug；
6. 最高分持久化：HighScoreStore 实现三档难度最高分读写，修复初始值与上传逻辑；
7. 单元测试：新增/扩展 19 个测试类共 205 个用例，覆盖模型、控制器、配置、难度、视图等模块，全部通过（0 失败 0 错误）；
8. 文档同步：更新需求规格说明书、详细设计说明书，与代码实现保持一致。

## AI 使用记录
| 生成目标 | 提示词要点 | 核对发现 | 处理 |
| --- | --- | --- | --- |
| 受击特效 HitEffect | 受击闪白/短时无敌/不侵入核心逻辑 | 初版特效触发时机异常 | 修正触发与渲染时序后采纳 |
| 炸弹道具动画 BombWave | 爆炸波扩散动画/伤害判定/数值可配 | 波及范围数值失衡 | 调整数值并新增 BombWaveTest 后采纳 |
| 难度系统 | 三档难度/配置驱动/读取 config.properties | 难度参数未随竖版战场同步调整 | 统一调参并补充 DifficultyTest 后采纳 |
| 窗口等比缩放适配 | 按屏幕尺寸等比缩放/小屏完整显示 | 部分小屏仍显示不全 | 改为按比例计算缩放后采纳 |
| 盾道具无敌帧 | 破盾后短暂无敌/防连续掉血 | 无敌帧时长过短 | 延长无敌帧并补 PlayerTest 后采纳 |

## 问题与风险
- 暂停状态下背景仍持续滚动 → 已修复，GameView 帧循环加入暂停状态判断。
- 首次开始游戏不显示第一关 → 已修复初始化顺序问题。
- 受击特效初版显示异常 → 已修复，并做回归验证。
- 最高分文件初始值与上传逻辑不一致 → 已统一初始化为 0 并修复上传。
- Maven surefire 运行出现 Manifest-JAR 绝对路径 classpath 警告（D:\dev\maven-repo）→ 属本地环境警告，不影响测试结果，后续可加 `-Djdk.net.URLClassPath.disableClassPathURLCheck=true` 消除。

## 明日计划
- 继续完善关卡与道具系统联调，优化交互细节；
- 全量回归测试并整理测试报告；
- 推进文档定稿与正式版本提交。
