# 日报 Day04（日期：2026-09-10）  组：廖尉钧，孙浩凡，孙嘉晟，艾奕舟，霍睿深

## 今日完成
1. 完成状态机设计，输出主状态机+游戏结束弹窗子状态机转移表与PlantUML图，明确入口守卫规则与全链路状态流转路径；
2. 完成核心类方法级详细设计，输出PlaneGameModel、GameController、GameView三类方法设计表，明确方法签名、前置后置条件与设计落点；
3. 实现三层核心骨架代码：Model层补全碰撞检测、敌机生成、计分与生命值逻辑，Controller层实现AnimationTimer主循环与事件转发，View层完成基础画面渲染与键盘事件监听；
4. 完成分层红线校验，全局搜索确认Model层零JavaFX引入，view→controller→model单向依赖关系符合架构规范；
5. 完成骨架代码运行验证，可正常启动游戏窗口，跑通玩家移动射击、敌机生成下落、碰撞计分、游戏结束与重开完整流程；
6. 输出《详细设计说明书》v1.0初稿，包含类方法设计、状态机、核心算法、设计权衡、AI核对说明共8章完整内容；
7. 新建`feature/detail-design`开发分支，完成全部设计文档与代码提交，合并至master并打tag `v0.3-lld`。

## AI 使用记录
| 生成目标 | 提示词要点 | 核对发现 | 处理 |
| --- | --- | --- | --- |
| 状态机PlantUML图 | 主状态+子状态机/转移标注触发事件/入口守卫规则 | 初始缺少游戏结束后操作拦截分支 | 补充结束状态守卫逻辑后采纳 |
| 方法级详细设计表 | 公私方法区分/前置后置条件/对应需求编号 | 部分内部方法未标注设计落点 | 补充设计理由与职责说明后采纳 |
| 三层骨架代码 | 分层约束/Model纯Java/Controller零业务/View仅渲染 | Model层意外引入JavaFX矩形类 | 替换为自定义坐标矩形碰撞实现，确保零界面依赖后采纳 |
| 编码规范核对 | 10条规范逐条校验/问题行号标注/修正建议 | 存在未解释魔法数字、遍历删除风险 | 提取命名常量、改用迭代器移除后采纳 |

## 问题与风险
- Model层初始碰撞逻辑误用JavaFX矩形类，违反分层红线 → 已替换为自定义int坐标矩形判定，全局搜索验证零javafx引入。
- 主循环初始无帧频控制导致敌机生成过快 → 已添加帧计数器控制生成间隔，稳定60帧下每60帧生成一架敌机。
- 玩家边界判定失效，飞机可移出屏幕 → 已补充左右边界坐标校验，越界时位置不更新。

## 明日计划
- 开展Model层单元测试开发，覆盖初始化、操作、碰撞、状态守卫、重置等核心场景；
- 调试优化核心逻辑，修复边界判定与碰撞检测的细节问题；
- 完善详细设计说明书，补充测试方案与算法细节，定稿v1.0版本。

